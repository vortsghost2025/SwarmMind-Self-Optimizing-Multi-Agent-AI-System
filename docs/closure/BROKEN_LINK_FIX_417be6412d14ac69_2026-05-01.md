# Broken External Link Resolution — Node 417be6412d14ac69

**Timestamp:** 2026-05-01T03:15Z  
**Lane:** SwarmMind (coordination)  
**Issue:** `https://deliberateensemble.works/library/417be6412d14ac69` returned 404, causing contradiction signal in Nexus Graph

**Root Cause:**  
`src/app/library/[id]/page.tsx` `generateStaticParams` filter excluded:
- Papers repo entries (only `content_type==="paper"` included)
- High-authority data files (like `data` with high `verificationCount`/`authorityDepth`)

Node `417be6412d14ac69` ("The Rosetta Stone (Structure Index)") is a `data` file in the `papers` repo with `authorityDepth=70` and `verificationCount=41`. Not in static params → page not pre-rendered → 404 on Vercel.

**Fix Applied:**
- Add `e.repo === "papers"` condition (captures all papers entries)
- Add `(e.content_type === "data" && (e.verificationCount >= 10 || e.authorityDepth >= 70))`
- Increase slice limit from 650 → 1000

**Commit:** `5bc8916` (self-organizing-library:main) pushed  
**Verification:** `curl -s -o /dev/null -w "HTTP %{http_code}\n" https://deliberateensemble.works/library/417be6412d14ac69` → `HTTP 200`

**Graph Impact:**  
Node remains CONFLICTED (6 CONTRADICTS edges) — that status is accurate. The broken link contradiction is resolved; any remaining contradictions are genuine semantic conflicts.

**Status:** ✅ Resolved
